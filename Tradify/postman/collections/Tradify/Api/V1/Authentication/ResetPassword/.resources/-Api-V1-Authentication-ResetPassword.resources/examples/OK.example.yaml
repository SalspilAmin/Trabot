$kind: http-example
request:
  url: "{{baseUrl}}/Api/V1/Authentication/ResetPassword"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "emailOrPhone": "string",
        "password": "string",
        "confirmPassword": "string"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
